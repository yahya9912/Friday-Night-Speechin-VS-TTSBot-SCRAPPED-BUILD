
function createPost() {
    FlxG.mouse.useSystemCursor = true;
    state.menuItems.y;
//    state.bg.scale.set(1,1);
    state.bg.scale.x = 0.5;
    state.bg.scale.y = 0.5;
    //state.menuItems.x = 0;
    //state.menuItems.menuItem.y = (state.i * 100) + state.offsetY;
}
